version: 2.1

setup: true

orbs:
  dynamic-message-filter: danielholdsworth/dynamic-message-filter@1.0.0

workflows:
  setup-workflow:
    jobs:
      - dynamic-message-filter/filter:
          mapping: |
            api
            auth
            e2e
            unit
